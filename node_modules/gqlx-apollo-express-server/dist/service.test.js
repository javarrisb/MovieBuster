"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const service_1 = require("./service");
describe('createService', () => {
    it('can create a simple service with no API usage', () => {
        const source = `
      type Query {
        hello(name: String): String {
          name ? 'Hello ' + name : 'Hello World'
        }
      }
    `;
        const data = {};
        const service = (0, service_1.createService)('default', source, data);
        expect(service.name).toBe('default');
        expect(service.data).toBe(data);
    });
    it('cannot create a service with invalid (empty) schema', () => {
        const source = ``;
        const data = {};
        expect(() => (0, service_1.createService)('default', source, data)).toThrow();
    });
});
//# sourceMappingURL=service.test.js.map