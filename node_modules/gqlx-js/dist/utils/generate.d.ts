import { Connectors, AvailableApi, SchemaResolvers } from '../types';
export declare function generate(name: string, connectors: Connectors): {
    resolvers: SchemaResolvers;
    createService(api: AvailableApi): (type: string, field: string, data: any) => any;
};
