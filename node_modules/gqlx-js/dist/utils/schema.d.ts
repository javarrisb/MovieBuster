import { Position, DynamicGqlSchema, GqlResolvers } from '../types';
export declare function createEmptyResolvers(): GqlResolvers;
export declare function extractResolvers(input: string, resolvers: GqlResolvers): Position[];
export declare function parseDynamicSchema(input: string): DynamicGqlSchema;
