import { Token, DocumentNode } from 'graphql';
import { Position } from '../types';
export declare function getName(tokens: Array<Token>): string | undefined;
export declare function getMode(tokens: Array<Token>, modes: Array<string>, current: string | undefined): string | undefined;
export declare function getArguments(ast: DocumentNode, typeName: string, fieldName: string): string[];
export declare function convertToPureGql(source: string, positions: Array<Position>): string;
