import { Expression } from 'estree';
import { ExpressionNode } from './ast';
export declare function awaitCall(node: Expression, ancestors: Array<ExpressionNode>, variables: Array<string>, start?: number): void;
