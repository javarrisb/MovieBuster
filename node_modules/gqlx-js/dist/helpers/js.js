"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getNames = exports.pushName = void 0;
function pushName(param, names) {
    switch (param.type) {
        case 'Identifier':
            names.push(param.name);
            break;
        case 'ArrayPattern':
            names.push(...getNames(param.elements));
            break;
        case 'AssignmentPattern':
            pushName(param.left, names);
            break;
        case 'ObjectPattern':
            for (const property of param.properties) {
                if (property.type === 'Property') {
                    pushName(property.value, names);
                }
            }
            break;
        case 'RestElement':
            pushName(param.argument, names);
            break;
        case 'MemberExpression':
            break;
    }
}
exports.pushName = pushName;
function getNames(params) {
    const names = [];
    for (const param of params) {
        if (param) {
            pushName(param, names);
        }
    }
    return names;
}
exports.getNames = getNames;
//# sourceMappingURL=js.js.map